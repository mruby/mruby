# An error given when you call a method with invalid parameters.
# That means you call a method with less than the required parameters, more than the total parameters
# or a parameter has an incorrect type.
class ArgumentError < StandardError
end
