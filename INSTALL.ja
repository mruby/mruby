* コンパイル・インストール



以下の手順で行ってください．

  コンパイル前にcmake 2.8.8 (http://www.cmake.org)をインストールしてください。

  1. cmake . ; make ; make install を実行してコンパイルする

     以下のディレクトリにライブラリおよびバイナリが作成されます。

       * /usr/local/bin
       * /usr/local/lib
       * /usr/local/include

もし，コンパイル時にエラーが発生した場合にはエラーのログとマ
シン，OSの種類を含むできるだけ詳しいレポートを作者に送ってく
ださると他の方のためにもなります．


* 移植


以上